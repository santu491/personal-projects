import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  itemStyle: {
    borderRadius: 4,
    marginHorizontal: 5,
    width: 8,
    height: 8,
  },
  dotViewStyle: {
    flexDirection: 'row',
    justifyContent: 'center',
    alignContent: 'center',
  },
});
